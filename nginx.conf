server {
    listen 80;
    server_name localhost;
    location / {
        root /usr/share/nginx/html;
        index index.html index.htm;
        # This line is the magic that fixes Angular routing refreshes
        try_files $uri $uri/ /index.html; 
    }
}